
# Build output and local artefacts — regenerated by build.sh on every deploy.
site/
SOCIAL_QUEUE/
data/model_index.json
__pycache__/
*.pyc
# local tooling only — CI uses `npx wrangler` and pip, neither needs these
node_modules/
package.json
package-lock.json
.wrangler/
data/wiki_specs.json
